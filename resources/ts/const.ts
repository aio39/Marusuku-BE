const PORT = 3939
const LOCAL_URL = `localhost:${PORT}`

const navList: { name: string; url: string }[] = [
    { name: "DashBoard", url: "/" },
    { name: "Page", url: "/page" },
];

export { PORT, LOCAL_URL, navList };
